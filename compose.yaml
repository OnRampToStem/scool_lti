services:
  lti:
    build: .
    image: scool_lti
    env_file:
      - .env
    environment:
      SCOOL_DEBUG: True
      SCOOL_PORT: 8443
    extra_hosts:
      - "canvas.docker:10.20.95.27"
    ports:
      - "8443:8443"
    volumes:
      - ./scool:/app/scool
